# Keep the actual OwnTone process in the foreground and restart it if AirPlay
# playback triggers a process-level failure. The official container otherwise
# keeps OpenRC (PID 1) alive while the OwnTone service remains unavailable.
supervisor="supervise-daemon"
command_args="-f"
respawn_delay=3
respawn_max=0
